// Import React and TodoCounter.css
import React from "react";
import "./css/TodoCounter.css";

// Define the TodoCounter component
function TodoCounter() {
  return <h2 className="TodoCounter">Lista de Tareas</h2>; // Return a heading element with the class name "TodoCounter" and the text "Lista de Tareas"
}

// Export the TodoCounter component
export { TodoCounter };
